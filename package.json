{
  "name": "thetracker-web",
  "private": true,
  "description": "TheTracker — Cloudflare Worker + static site (mrzahi.com)",
  "dependencies": {
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
